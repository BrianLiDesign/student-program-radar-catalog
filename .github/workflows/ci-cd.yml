name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.13"]

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt -r requirements-dev.txt

      - name: Lint and format check
        run: |
          python -m ruff check scripts config test_workflow.py test_scrape_programs.py test_dashboard.py
          python -m ruff format --check scripts config test_workflow.py test_scrape_programs.py test_dashboard.py

      - name: Compile Python sources
        run: python -m compileall -q scripts config

      - name: Run tests
        run: python -m pytest -q

      - name: Validate catalog data
        run: python scripts/validate_data.py

      - name: Run end-to-end verification
        run: python scripts/test_end_to_end.py
